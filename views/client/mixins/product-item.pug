mixin product-item(item)
    .col-4.mb-3
        .product-item
            .inner-image
                a(href=`/products/detail/${item.slug}`)
                    img(src=item.thumbnail, alt=item.title, text-align=center)
                if(item.featured == "1")
                    .inner-featured Nổi bật
            .inner-content
                h3.inner-title 
                    a(href=`/products/detail/${item.slug}`) #{item.title}
                .inner-price-new #{item.priceNew}$
                //- .inner-price-old #{item.price}$
                //- .inner-discount -#{item.discountPercentage}%
                .inner-button
                form(
                    action=`/cart/add/${item.id}`,
                    method="POST"
                )
                    input(
                        class="form-control mb-10 d-none"
                        type="number"
                        name="quantity" 
                        value="1"
                        min="1"
                        max=item.stock
                        )
                    button(type="submit" class="btn btn-info  btn-block") Thêm vào giỏ hàng